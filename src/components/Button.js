import React, { Component } from "react";

class Button extends Component {
    render() {
        return (
            <button>
                I am button from REMOTE app.
            </button>
        );
    }
}

export default Button;
